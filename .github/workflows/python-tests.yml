name: Python Tests

on:
  push:
    paths:
      - 'python/**'
      - 'printemps/**'
      - '.github/workflows/python-tests.yml'
  pull_request:
    paths:
      - 'python/**'
      - 'printemps/**'
      - '.github/workflows/python-tests.yml'

jobs:
  test-python:
    name: Python Binding Tests (${{ matrix.os }}, Python ${{ matrix.python-version }})
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest, windows-latest]
        python-version: ['3.9', '3.10', '3.11', '3.12', '3.13', '3.14']
    steps:
      - uses: actions/checkout@v6

      - name: Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}

      # Set up MSVC developer command prompt on Windows for C++ compilation
      - name: Set up MSVC (Windows)
        if: runner.os == 'Windows'
        uses: ilammy/msvc-dev-cmd@v1
        with:
          arch: x64

      - name: Install Ninja (Windows)
        if: runner.os == 'Windows'
        uses: seanmiddleditch/gha-setup-ninja@v5

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip build pytest

      - name: Vendor C++ headers and build package
        run: |
          cd python
          python tools/vendor.py
          python -m build

      - name: Install built wheel (Unix)
        if: runner.os != 'Windows'
        run: |
          python -m pip install python/dist/*.whl

      - name: Install built wheel (Windows)
        if: runner.os == 'Windows'
        shell: pwsh
        run: |
          python -m pip install (Get-ChildItem python/dist/*.whl).FullName

      - name: Run pytest
        run: |
          cd python
          pytest tests
